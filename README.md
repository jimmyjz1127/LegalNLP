# CS4099-LegalNLP
Scripts for training HuggingFace transformer models for text-pair classification and masked language modelling using legal corpus.

# Library Installations 

pip install -U sentence-transformers 

pip install --user -U nltk

pip install --user -U scikit-learn 

